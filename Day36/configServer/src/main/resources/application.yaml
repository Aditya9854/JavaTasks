
spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/username/config-repo.git
          search-paths: '*'
          clone-on-start: true
server:
  port: 8888
