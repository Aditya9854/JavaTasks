
spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/config-repo
        #git:
        #  uri:   #git url
         # clone-on-start: true
server:
  port: 8888
