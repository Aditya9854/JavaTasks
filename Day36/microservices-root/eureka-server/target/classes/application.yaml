spring:
  application:
    name: eureka-server
  config:
    import: "optional:configserver:http://localhost:8888" #fetching config
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761

